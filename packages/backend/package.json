{
    "name": "@goal-mandala/backend",
    "version": "0.1.0",
    "description": "Hono + Lambda バックエンド",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsx watch src/index.ts",
        "clean": "rm -rf dist",
        "type-check": "tsc --noEmit",
        "lint": "eslint src --ext ts --report-unused-disable-directives --max-warnings 0",
        "test": "jest",
        "test:coverage": "jest --coverage"
    },
    "dependencies": {
        "@goal-mandala/shared": "workspace:*",
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "@types/jest": "^29.5.0",
        "@types/node": "^20.11.0",
        "jest": "^29.5.0",
        "ts-jest": "^29.1.0",
        "tsx": "^4.7.0",
        "typescript": "^5.3.3"
    },
    "files": [
        "dist"
    ]
}
