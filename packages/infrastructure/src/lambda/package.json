{
  "name": "database-iam-setup-lambda",
  "version": "1.0.0",
  "description": "Lambda function for IAM database authentication setup",
  "main": "database-iam-setup.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "@aws-sdk/client-secrets-manager": "^3.450.0",
    "@aws-sdk/client-rds": "^3.450.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.7",
    "typescript": "^5.2.2"
  }
}
